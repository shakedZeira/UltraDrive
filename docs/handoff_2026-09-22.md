# UltraDrive — Session Handoff (2026-09-22)

Start a new opencode session here (Godot 4.7.2, root = this repo). Read AGENTS.md
before work — it has the HEADLESS WORKFLOW gates, BLENDER MCP notes, and style
rules. Git tree is clean except the untracked files listed at the bottom.

## Live tooling (just set up in the previous session)

- **token-slim plugin (active)** — token-reduction plugin for opencode, loaded
  from `D:\AI Projects\UltraDrive\.opencode\plugins\token-slim.js` (a wrapper
  that `await import()`s the built dist at `D:\AI Projects\OpenCode Token
  minimizer\dist\index.js`). It trims old tool outputs + compresses built-in
  tool descriptions; verified LIVE in the prior session (markers
  `... [output trimmed by token-slim]` visible in tool results). If it stops
  working after a plugin rebuild, re-check the wrapper; the config `plugin`
  array at `C:\Users\LotusSeven\.config\opencode\opencode.json` must stay
  empty (it's npm-packages-only; the `file://` entry was the original dead
  end).

## Shipped recently (all pushed)

- `6dd68bb` — **region-crossing freeze fixed**: 256 m regions + capped
  per-frame ring adds + deferred rebuilds (crossing blip 1.09 s → ~80 ms).
- `7ffb524` — preset-aware `tools/perf_probe.gd` (`PERF_QUALITY=low|medium|high`)
  + PRESET GATE verified (Low 93 / Medium 65 / High 57 fps @1080p GTX 970).
- `d8610ef`, `6f9f247`, `7825def` — open-world perf milestone (7.7 → 94.9 fps Low).
- Recent full suite: 533 tests | 0 errors | 0 failures | 0 flaky (only the
  known pre-existing `test_race_loop.gd::test_total_time_is_relative_to_race_start`
  timing flake can appear; it fails on a clean baseline too).

## Open TODOs (user-priority order)

1. **Fix high-speed handling stability** — user report (verbatim): *"the cars
   handling is really slippery at high speed, it doesnt have any stability."*
   Work in `scripts/vehicle/` (`vehicle_physics.gd`, `wheel_physics.gd`
   Pacejka, `tire_model.gd` grip hooks). Goal: high-speed stability WITHOUT
   killing the arcade low-speed feel or the FUN playtest flows. Gates:
   `test_surface_grip`, `test_drive_feel` / `test_hardening` suites stay green
   (`test_hardening.gd::test_arcade_speed_clamp` is a pre-existing timing
   flake — verify at baseline before blaming). If a test asserts the current
   slippery behaviour, STOP and confirm before editing it (red-flag rule).
   Use ONE sub-agent per task; one Godot process at a time; watchdog every
   headless run.
2. **Dynamic car sounds** — user report: *"the car sounds really static …
   we can use gemini or other tools like sound models to correctly make car
   sounds (dont have to bo real)."* Audit EngineAudio (S12 engine beds);
   goal is RPM/throttle/gear-reactive layered OR synthesized sound (synthetic
   is fine). File location: locate on dispatch.
3. **Backlog** — `docs/plans/match_fh6_gt7_plan.md` S13–S17 (post these two).

## Cancelled/on-hold (do NOT resume unless asked)

- **Static far-terrain vista** work was cancelled mid-way after ~2+ hours.
  Design IS complete and user-approved (approach chosen: static far-terrain
  layer): see `docs/plans/terrain_vista_plan.md` (Phases 0–5, -1 m depth
  bias, 9×SIZE_1024 @ vertex_spacing 8, camera far 4000→24000, worker
  bake). Half-written artifacts exist untracked (below). Can be resumed later
  from Phase 1 if the user wants the horizon back.

## Untracked files in the tree (untouched; NOT committed — do not `git add -A`)

- `.opencode/plugins/token-slim.js` — plugin wrapper (keep; also a copy at
  `C:\Users\LotusSeven\.config\opencode\plugins\token-slim.js`).
- `docs/plans/terrain_vista_plan.md` — vista design doc (keep; on-hold).
- `scripts/world/terrain_vista_baker.gd`, `tools/diag_vista_api.gd`,
  `tools/diag_vista_api.tscn` — half-implemented vista code (on-hold).

## Verification commands (from AGENTS.md — reuse, don't rediscover)

```bat
"D:\Godot\Godot_v4.7.2-stable_win64.exe" --headless --import .  2>&1 | findstr /i "SCRIPT ERROR Parse Error Failed to load"

"D:\Godot\Godot_v4.7.2-stable_win64.exe" --headless -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests > _gdunit.txt 2>&1
findstr /c:"Overall Summary:" _gdunit.txt
```

Run the import probe FIRST (gdunit without it bails exit 103). One Godot
process at a time. Don't leak the Mistral API key in `~/.config/opencode/opencode.jsonc`.

## ADDENDUM (2026-09-22, pre-restart) — pick up here

- token-slim now gentler: budgets rebuilt in `D:\AI Projects\OpenCode Token
  minimizer` (src/utils.ts toolBudgets -> balanced ageTurns 4 / maxChars 12000,
  light 5/20000; dist rebuilt; 28/28 plugin tests green). Takes effect THIS
  restart.
- TODO #1 high-speed handling: analysis complete in prior session. ROOT CAUSE:
  `downforce_coefficient` (0.0) and `countersteer_assist` (800) are defined in
  car_config.gd but consumed NOWHERE (grep confirms zero consumers). Only the
  steering `speed_factor` (1 - kmh/200, clamp 0..0.7) + `angular_damp = 0.5`
  exist. The tire curve saturates ~3 deg slip, so the car has no grip margin or
  yaw discipline at speed -> "slippery, no stability."
  FIX DIRECTION: (a) implement aero downforce from `downforce_coefficient`
  (v^2 -> extra normal load into the Pacejka D term + a chassis downforce);
  (b) implement `countersteer_assist` as a yaw damper toward the kinematic yaw
  target (`forward_speed * tan(steer_angle) / wheelbase`), speed-tapered
  (ramps in above ~70 km/h), OFF during handbrake, capped so low-speed arcade
  drift is untouched. Set sensible per-car values in `resources/cars/*.tres`
  (defaults in car_config.gd stay 0.0 / 800). Sign check done: positive
  steer_angle => positive heading => positive target yaw, matches
  `angular_velocity.y`.
  GATES that must stay green: `tests/suites/test_surface_grip.gd`,
  `test_drive_feel.gd`, `test_hardening.gd` (its live-frame clamp test drives
  straight at top speed, so downforce/countersteer are safe). Use ONE sub-agent
  for this work; run the import probe FIRST then the gdunit suite; add a
  watchdog to every headless run; one Godot process at a time.
- TODO #2 dynamic car sounds: audit `scripts/vehicle/car_audio.gd` (S12 engine
  beds); goal RPM/throttle/gear-reactive layered or synthesized sound.
- Use ONE sub-agent per task (user requirement). Never `git add -A` (uid noise).